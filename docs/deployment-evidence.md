# Deployment Evidence

## Current Status

The repository includes:

- Dockerfile for containerization
- `docker-compose.yml` for staging-style execution
- GitHub Actions workflow for CI/CD
- deploy stage ready for Render using `RENDER_DEPLOY_HOOK_URL`
- local runtime proof in `artifacts/local-health-check.json`

## Evidence To Attach After Push/Deploy

Add the following before final submission:

1. Screenshot of successful GitHub Actions run
2. Screenshot of Docker container or staging deployment dashboard
3. Live staging URL or Render service URL
4. Screenshot of the running invoice dashboard

## Placeholder Live Link

`REPLACE_WITH_DEPLOYED_STAGING_LINK`
